# SCENE BOX Google Drive 설정

클라이언트 ID는 아직 비워 두었습니다. 아래 설정을 완료하기 전에도 사진 보관, 용량 표시, 사용자 분류는 정상 동작합니다. 실제 Google 계정 연결은 설정 후 확인해야 합니다.

## 1. Google Cloud 프로젝트와 API

1. [Google Cloud 콘솔](https://console.cloud.google.com/)에 사용할 Google 계정으로 로그인합니다.
2. 상단 프로젝트 선택 → **새 프로젝트**에서 이름을 정하고 생성한 프로젝트를 선택합니다.
3. **API 및 서비스 → 라이브러리**에서 **Google Drive API**를 검색하고 **사용**을 누릅니다.

## 2. OAuth 동의 화면과 테스트 사용자

1. **Google Auth Platform → Branding(브랜딩)**으로 이동합니다. 처음이라면 **시작하기**를 누릅니다.
2. 앱 이름 `SCENE BOX`, 사용자 지원 이메일, 개발자 연락 이메일을 입력합니다.
3. 개인 Gmail 계정으로 사용할 경우 **Audience(대상) → External(외부)**를 선택합니다.
4. 게시 상태는 우선 **Testing(테스트)**으로 두고, **Audience → Test users(테스트 사용자) → Add users**에 실제 연결할 Gmail 주소를 등록합니다.
5. **Data Access(데이터 액세스) → Add or remove scopes**에서 다음 범위를 추가합니다.

   `https://www.googleapis.com/auth/drive.file`

   이 앱이 생성하거나 접근 권한을 받은 파일을 관리하는 범위입니다. 전체 Drive 접근 범위를 추가할 필요는 없습니다.

설정 메뉴는 [Google OAuth 동의 화면 공식 안내](https://developers.google.com/workspace/guides/configure-oauth-consent)에 따라 진행할 수 있습니다. 다른 사람에게 공개 배포할 때는 대상·게시 상태와 Google이 요구하는 검증을 별도로 확인하세요.

## 3. 웹 클라이언트 생성

1. **Google Auth Platform → Clients(클라이언트) → Create client(클라이언트 만들기)**를 선택합니다.
2. 애플리케이션 유형은 **Web application(웹 애플리케이션)**입니다.
3. **Authorized JavaScript origins(승인된 JavaScript 출처)**에 아래 주소만 입력합니다.

   ```text
   https://cjy00852.github.io
   ```

   `/scene-box/` 경로나 끝의 `/`는 붙이지 않습니다. 이 앱은 브라우저 팝업 토큰 방식을 사용하므로 승인된 리디렉션 URI는 필요하지 않습니다.
4. 생성된 **클라이언트 ID**를 복사합니다. 클라이언트 보안 비밀은 사용하지 않습니다.

공식 참고: [Drive JavaScript 시작하기](https://developers.google.com/workspace/drive/api/quickstart/js).

## 4. SCENE BOX에 ID 입력

저장소 루트의 **[google-config.js](google-config.js) 3번째 줄**에서 빈 따옴표 안에 발급받은 클라이언트 ID를 그대로 넣습니다.

```js
window.SCENE_GOOGLE_CLIENT_ID = "";
```

저장 후 커밋·푸시하고 GitHub Pages 배포가 완료되면 앱을 새로고침합니다. 이 ID는 공개 웹 설정값이며, **비밀번호·클라이언트 보안 비밀·액세스 토큰은 이 파일에 넣지 않습니다.** 별도 서버나 API 키는 필요하지 않습니다.

## 5. 연결 및 확인

1. SCENE BOX의 **관리 → Google Drive 동기화 → Google Drive 연결 / 재연결**을 누릅니다.
2. 테스트 사용자로 등록한 계정을 선택하고 요청한 권한에 동의합니다.
3. 기존 자료도 자동 대기열에 포함됩니다. 연결하면 대기 중인 모든 원본의 업로드가 시작됩니다.
4. Drive의 `SCENE BOX` 폴더와 앱의 대기·진행 중·성공·실패 건수 및 마지막 동기화 시간을 확인합니다.
5. 자료의 멤버 또는 사용자 분류를 바꾸면 동일한 Drive 파일 ID를 유지하면서 폴더와 파일명이 변경됩니다. 실패 항목은 **실패 항목 다시 시도**로 재개할 수 있습니다.

## 동작 범위

- **기기 → Drive 단방향 업로드**입니다. Drive에서 삭제하거나 수정한 내용을 기기로 내려받는 양방향 동기화는 하지 않습니다. 로컬 자료를 삭제해도 Drive 원본은 삭제하지 않습니다.
- 사용자 분류가 있으면 그 분류 폴더를 우선 사용합니다. 기존 자료에 버블·멤버십이 동시에 지정된 경우 둘 다 보존하되, 저장된 분류 목록의 첫 번째 폴더 한 곳에만 올립니다. 새로 선택할 때는 선택 목록의 순서를 따릅니다.
- 분류가 없으면 멤버 1명 → 멤버 폴더, 2~4명 → `2인 이상/원이+제나` 같은 조합 폴더, 전원·단체 → `단체`, 미분류 → `기타`입니다. 조합 순서는 원이·리브·미나미·메이·제나로 고정됩니다.
- 분류를 삭제하면 해당 지정만 기타로 바뀝니다. 기존 Drive 폴더와 원본은 삭제하지 않으며, 연결 중이면 자료를 기타 폴더로 이동합니다.
- Drive 파일명만 `260920_버블_원이_001.jpg`처럼 변경합니다. 촬영일은 JPEG/PNG의 EXIF, MP4/MOV의 생성 메타데이터를 읽으며, 앱에서 지정한 촬영일도 사용합니다. 읽을 수 있는 촬영일이 없으면 등록일, 등록일도 없으면 최초 동기화일을 `등록일260920`으로 표시합니다. 지원하지 않는 형식이나 메타데이터가 제거된 파일도 등록일로 업로드됩니다. 원본 파일 자체는 수정하지 않습니다.
- 화면을 보고 있고 연결이 유효할 때 자동 업로드합니다. 백그라운드·오프라인으로 바뀌면 중단하고 돌아오면 이어서 처리합니다. 화면 꺼짐 방지는 브라우저가 Wake Lock을 지원하고 허용할 때만 적용됩니다.
- 브라우저 팝업 토큰 방식에는 영구 자동 로그인이 없습니다. 같은 탭의 새로고침은 유효한 연결을 재사용하지만, 토큰 만료 또는 새 브라우저 세션에서는 **재연결 버튼을 눌러야 합니다**. 앱을 완전히 닫은 동안 업로드는 실행되지 않습니다. 로그인 팝업을 자동 반복하지 않습니다. [Google 토큰 모델 안내](https://developers.google.com/identity/oauth2/web/guides/use-token-model).
- 대기열·Drive 파일 ID·재시도 상태는 IndexedDB에 저장됩니다. 작업은 최대 5회까지 간격을 늘려 재시도하고 이후 수동 재시도를 기다립니다. 토큰은 탭의 sessionStorage에만 저장하며 전체 백업에는 넣지 않습니다.
- 저장 용량은 등록한 **원본 Blob 크기의 합계**입니다. 썸네일·배경·Drive 사용량은 포함하지 않습니다. 기존 자료는 최초 1회 파일 크기를 수집한 뒤 추가·삭제 시 증감하여 저장합니다. 전체 백업에는 사용자 분류도 포함됩니다.
- 기기나 브라우저의 사이트 데이터를 지우면 로컬 자료와 대기열도 지워지므로 기존 전체 백업 기능을 계속 사용할 수 있습니다. 계정을 바꾸면 새 계정에 별도로 업로드합니다.

## 개발 검증

```sh
npm install
npx playwright install chromium
npm test
```

Windows에 Edge가 설치되어 있으면 PowerShell에서 브라우저 다운로드 없이 실행할 수 있습니다.

```powershell
$env:SCENE_TEST_BROWSER = 'msedge'
npm test
```

테스트는 별도 브라우저 프로필의 가짜 Google API로 데이터 마이그레이션, 용량 증감, 분류, 파일 이동, 업로드 재개, 인증 만료 등을 확인합니다. 실제 Google 로그인을 검증하는 테스트는 아닙니다.
